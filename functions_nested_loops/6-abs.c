#include "main.h"
/**
 * _abs - display the absolute value of a number
 * Return: return r
 * @r: is a integer
 */
int _abs(int r)
{
	if (r < 0)
	{
		r = r * -1;
		return (r);
	}
	else
	{
		return (r);
	}
}
